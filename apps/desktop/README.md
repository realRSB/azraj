# Desktop

The desktop app currently lives in `../../electron` and is launched from the root scripts:

```bash
npm run desktop:dev
```

This folder exists so the repo can grow toward an app-oriented monorepo without moving the Electron shell before the public web app is ready.
