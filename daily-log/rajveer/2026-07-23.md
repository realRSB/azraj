# Daily Log - 2026-07-23

Owner: Rajveer

## Summary

Today focused on turning Azraj from a local iMessage agent into a more complete public onboarding and dashboard product while keeping local development as the near-term operating mode.

## Work Completed

- Built the `public-signup-onboarding` branch with a public Azraj landing page and Start Connecting flow.
- Added phone-based signup/sign-in using Sendblue-delivered OTPs and Convex-backed sessions.
- Linked verified phone numbers to iMessage conversation IDs so dashboard data is scoped per user.
- Added a user-facing web dashboard for messages, memory, usage/cost, automations, and check-ins.
- Reworked the dashboard surface to feel closer to the existing local dashboard while preserving the light Azraj aesthetic.
- Scoped new memory writes and recall by conversation ID to avoid cross-user memory leakage.
- Added a local web dashboard runbook at `apps/web/README.md`.
- Opened draft PR #6: https://github.com/realRSB/azraj/pull/6
- Explored persistent deployment options with Railway, Azure, and DigitalOcean.
- Attempted Railway deployment, but Railway blocked project creation because the selected workspace trial is expired.
- Decided to keep using local development for now rather than deploy persistent infrastructure immediately.

## Validation

- `npx convex codegen` passed during the public dashboard work.
- `npm run typecheck` passed.
- `npm test` passed with 78 tests.
- `npm run web:build` passed.
- Local server smoke test passed on alternate port `3466`.

## Blockers

- Persistent Railway deploy is blocked until the Railway workspace has an active plan.
- Public always-on access is deferred; current mode remains local dev with ngrok and Sendblue webhook checks.

## Next Steps

- Review PR #6 and decide what should merge into `main`.
- Continue polishing the onboarding, dashboard, and accountability loop locally.
- Revisit persistent deployment after the product flow is more stable or an Azure/DigitalOcean path is chosen.

## Notes

- Local restart flow is `npm run dev` plus `npm run web:dev`.
- After restarting local dev, run `npm run sendblue:webhook:check` and update with `npm run sendblue:webhook` if ngrok changed.
