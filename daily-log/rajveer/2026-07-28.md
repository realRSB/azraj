# Daily Log - 2026-07-28

Owner: Rajveer

## Summary

Today focused on making public auth production-ready and separating local testing from the live Azraj number.

## Work Completed

- Shifted the consumer-facing dashboard login path from phone OTP to iMessage-first magic links.
- Linked inbound Sendblue texts to public users so Azraj can send short-lived private dashboard links.
- Kept OTP endpoints as the returning-user sign-in path while moving signup/start connecting to a fresh bracketed iMessage join code.
- Stored signup codes hashed in Convex with one-time-use consumption by the phone number that sends the exact bracketed message.
- Polished returning sign-in so send code transitions into a dedicated one-time OTP entry card.
- Added focused coverage for magic-link URL building, token hashing, link intent detection, and public route allowlisting.
- Made the public web app read the active Azraj number from the running server so local shows the dev Sendblue line and prod shows the Railway line.
- Changed verified sign-in to land on the dashboard instead of reusing the signup join-code card.
- Deployed Convex production after Railway logs showed the join-code mutation failing against an outdated prod schema/functions deployment.

## Validation

- `npm run typecheck`
- `npm run web:build`
- `npm test`
- Live `POST https://azraj.tech/api/public-auth/join/start` returned a fresh join code.
- Live `POST https://azraj.tech/api/public-auth/start` returned `200 OK` for the returning sign-in flow.

## Blockers

- OTP rate limiting is intentionally still active, so repeated sign-in tests on the same phone can temporarily block new codes.

## Next Steps

- Keep local testing on the dev Sendblue number and production testing on the Railway/prod Sendblue number.
- Continue using signup bracket codes for first-time users and OTP only for returning dashboard sign-in.

## Notes

- `daily/2026-07-28.md` was moved into `daily-log/rajveer/2026-07-28.md` to match the project daily-log convention.
