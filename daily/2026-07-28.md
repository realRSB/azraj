# 2026-07-28

## public auth

- shifted the consumer-facing dashboard login path from phone OTP to iMessage-first magic links.
- inbound Sendblue texts now link the phone to the public user and can trigger a short-lived private dashboard link.
- kept the existing OTP endpoints in place as a hidden fallback, but the web onboarding now tells users to text Azraj and open the link Azraj sends back.
- split onboarding into signup/start connecting with a fresh bracketed iMessage join code and returning sign-in with phone OTP.
- signup codes are issued server-side, stored hashed in Convex, one-time use, and consumed only by the phone number that sends the exact bracketed message.
- polished returning sign-in so send code transitions into a dedicated one-time OTP entry card instead of iMessage-first guidance.
- added focused coverage for magic-link URL building, token hashing, link intent detection, and public route allowlisting.

## checks

- `npm run typecheck`
- `npm run web:build`
- `npm test`
